#ifndef BACKLIGHT_HPP
#define BACKLIGHT_HPP

#include "tim.h"

extern volatile int backlight_delay;
// int BACKLIGHT_STATE;
static enum {
    _OFF,
    _LOW,
    _MID,
    _HIGH
} BACKLIGHT_STATE;

void backlightSetup(void);
void backlightLoop(void);

void fadeScreen(int target_brightness);

extern int current_brightness;
extern int desired_brightness;

extern int low_brightness;
extern int mid_brightness;
extern int hi_brightness;
extern int off;

#endif // BACKLIGHT_HPP
